# 快速开始 - 3 分钟配置

## 第一步：配置权限（30 秒）

工作流设计为**自主执行**，但默认需要手动确认。配置权限后，工作流将自动：

- ✅ 搜索并安装技能
- ✅ 运行测试
- ✅ 提交代码
- ✅ 并行执行任务

**一键配置：**

```bash
cd /path/to/自动工作流
source setup-permissions.sh
```

**或手动配置：**

复制 `docs/PERMISSIONS.md` 中的配置到 `.claude/settings.local.json`

## 第二步：创建项目结构（1 分钟）

```bash
# 1. 创建工作流目录
mkdir -p .workflow

# 2. 定义项目目标
cat > .workflow/GOAL.md << 'EOF'
# 项目理想状态

## 产品愿景
[你的产品愿景]

## 成功标准
- [ ] 标准 1
- [ ] 标准 2
EOF

# 3. 创建初始任务列表
cat > .workflow/tasks.json << 'EOF'
{
  "tasks": [
    {
      "id": "task-001",
      "title": "�项目结构",
      "status": "pending",
      "priority": 1,
      "description": "创建项目基础目录和配置",
      "steps": [
        "创建目录结构",
        "初始化 Git",
        "创建 README"
      ],
      "acceptance_criteria": [
        {
          "criterion": "目录结构完整",
          "type": "manual",
          "passed": false
        },
        {
          "criterion": "Git 已初始化",
          "type": "auto",
          "verify": "git status",
          "passed": false
        }
      ]
    }
  ]
}
EOF

# 4. 创建当前状态文件
cat > .workflow/REALITY.md << 'EOF'
# 当前状态

## 已实现
- 工作流初始化

## 待改进
- 完成所有任务

## 最后更新
$(date +%Y-%m-%d)
EOF
```

## 第三步：开始工作流（10 秒）

在 Claude Code 中运行：

```
/workflow:continue
```

工作流将：
1. 读取 `.workflow/GOAL.md` 了解目标
2. 读取 `.workflow/tasks.json` 找到待办任务
3. 自动执行 `task-001`（初始化项目结构）
4. 运行验收标准（`git status`）
5. 自动提交代码

## 第四步：添加更多任务（30 秒）

```bash
/workflow:add-task
```

或手动编辑 `.workflow/tasks.json`：

```json
{
  "tasks": [
    {
      "id": "task-001",
      "title": "�项目结构",
      "status": "completed",
      "priority": 1,
      "completed_at": "2026-02-14T10:00:00Z"
    },
    {
      "id": "task-002",
      "title": "实现核心功能",
      "status": "pending",
      "priority": 2,
      "dependencies": ["task-001"],
      "description": "实现产品核心功能",
      "context": "使用测试驱动开发",
      "acceptance_criteria": [
        {
          "criterion": "核心功能测试通过",
          "type": "auto",
          "verify": "pytest tests/test_core.py -v",
          "passed": false
        }
      ]
    },
    {
      "id": "task-003",
      "title": "编写文档",
      "status": "pending",
      "priority": 3,
      "dependencies": ["task-001"],
      "description": "编写 API 文档和用户指南",
      "acceptance_criteria": [
        {
          "criterion": "文档审查通过",
          "type": "manual",
          "passed": false
        }
      ]
    }
  ]
}
```

**注意：** `task-002` 和 `task-003` 都依赖 `task-001`，但它们之间没有依赖关系，**工作流会自动并行执行它们**！

## 第五步：继续工作（自动）

之后只需要运行：

```
/workflow:continue
```

工作流会：
- 🔍 **智能分析依赖**：找出可以并行的任务
- 🔍 **动态搜索技能**：根据任务类型自动搜索最合适的技能
- ⚡ **并行执行**：使用 Task 工具创建后台代理
- ✅ **自动验收**：运行测试并检查结果
- 📝 **自动提交**：使用 conventional commit 格式

## 查看进度

```
/workflow:status
```

会显示：
- 整体进度（X / Y 任务完成）
- 当前进行中的任务
- 下一个待办任务
- 每个任务的验收标准通过情况

## 常见问题

### Q: 为什么还要我确认？

**A:** 权限未配置。运行：

```bash
source setup-permissions.sh
```

或手动参考 `docs/PERMISSIONS.md` 配置。

### Q: 如何让任务并行执行？

**A:** 在 `tasks.json` 中定义独立任务：

```json
{
  "tasks": [
    {
      "id": "task-A",
      "title": "实现前端",
      "dependencies": ["task-001"]  // 只依赖 001
    },
    {
      "id": "task-B",
      "title": "实现后端",
      "dependencies": ["task-002"]  // 只依赖 002
    }
  ]
}
```

如果 `task-001` 和 `task-002` 都已完成，`task-A` 和 `task-B` 会自动并行执行！

### Q: 如何让工作流使用特定技能？

**A:** 在任务的 `context` 或 `description` 中使用明确的关键词：

```json
{
  "description": "创建响应式登录界面",
  "context": "需要现代化 UI 设计，使用 React 和 TailwindCSS"
}
```

工作流会提取关键词（"响应式"、"登录界面"、"React"），然后：
1. 检查是否有内置技能（如 frontend-design）
2. 如果没有，运行 `npx skills find "responsive frontend React"`
3. 选择安装量最高的技能
4. 自动安装并使用

### Q: 验收标准如何自动执行？

**A:** 在 `acceptance_criteria` 中设置 `type: "auto"`：

```json
{
  "criterion": "所有测试通过",
  "type": "auto",  // 自动执行
  "verify": "pytest tests/ -v",  // 要运行的命令
  "passed": false
}
```

工作流会自动运行 `pytest tests/ -v`，如果成功（exit code 0），标记为 `passed: true`。

## 下一步

- 📖 阅读完整文档：`CLAUDE.md`
- 📚 查看权限详解：`docs/PERMISSIONS.md`
- 🔀 查看并行示例：`examples/parallel-execution-example.md`
- 🔍 查看技能搜索示例：`examples/dynamic-workflow-example.md`

---

**开始使用：**

```bash
# 1. 配置权限
source setup-permissions.sh

# 2. 开始工作流
/workflow:continue
```

**就这么简单！** 🎉
